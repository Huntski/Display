import axios from 'axios'

export default {
    namespaced: true,

    actions: {
        login() {
            // Authenticate into anilist
        },

        callback() {
            // Callback from anlist
        }
    }
}